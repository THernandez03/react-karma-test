import React from 'react';
import ReactDOM from 'react-dom';
import Boton from '../private/components/Boton';

console.log(React);
console.log(ReactDOM);
console.log(Boton);

// describe('Dado que la aplicacion requiere todo lo necesario', () => {
//   it('entonces se renderiza el boton por defecto', () => {
//     const instance = ReactDOM.render(<Boton/>, document.getElementById('app'));
//     expect(instance.props.text).toEqual('Boton');
//   });
//
//   it('entonces se renderiza el boton y fue definido su texto', () => {
//     const instance = ReactDOM.render(<Boton text="CACA"/>, document.getElementById('app'));
//     expect(instance.props.text).toEqual('CACA');
//   });
//
//   it('entonces se renderiza el boton y fue definido su imagen', () => {
//     const instance = ReactDOM.render(<Boton src="http://www.placehold.it/350x350"/>, document.getElementById('app'));
//     expect(instance.props.src).toEqual('http://www.placehold.it/350x350');
//   });
// });
